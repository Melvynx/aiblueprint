---
title: configs backups list
description: List automatic configuration backups with reasons and paths.
command: aiblueprint configs backups list
---

`configs backups list` prints automatic backups.

```bash
aiblueprint configs backups list
```

Each backup includes a name, date, reason, trigger, folders, and filesystem path.
