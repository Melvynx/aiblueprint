---
title: configs backups load
description: Load an automatic backup and backup current folders first.
command: aiblueprint configs backups load
---

`configs backups load` restores a backup by name.

```bash
aiblueprint configs backups load <name>
```

The command creates a fresh backup of the current configuration before restoring the selected backup.
