---
title: configs backups create
description: Manually create an automatic-style backup of current config folders.
command: aiblueprint configs backups create
---

`configs backups create` creates a manual backup.

```bash
aiblueprint configs backups create [reason]
```

## Example

```bash
aiblueprint configs backups create "before premium sync"
```
