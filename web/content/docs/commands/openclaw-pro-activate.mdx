---
title: openclaw pro activate
description: Activate OpenClaw Pro with an access token.
command: aiblueprint openclaw pro activate
---

`openclaw pro activate` validates and saves an OpenClaw Pro token.

```bash
aiblueprint openclaw pro activate [token]
```

When the token is omitted, the command prompts for it interactively.
